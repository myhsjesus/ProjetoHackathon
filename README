Navegue até a pasta do backend no cmd e rode os seguintes comandos:

pip install flask-cors
pip install Flask SpeechRecognition requests

depois rode o server do back-end com python app_backend.py


para o front:
navegue para a psta FrontEnd no cmd e rode:
npm install

depois rode o front-end com:
npm start